{
  "name": "json-backend",
  "version": "1.0.0",
  "description": "A simple JSON backend using json-server",
  "main": "server.js",
  "scripts": {
    "start": "json-server db.json --port 8080"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
